<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <meta name="description" content="Marieteam coroporate" />
    <meta name="author" content="Marieteam" />
    <title>
      {% block title %}
        Welcome!
      {% endblock %}
    </title>
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>" />
    {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
    {% block stylesheets %}
      {{ encore_entry_link_tags('app') }}
    {% endblock %}
  </head>
  <body id="page-top">
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top" id="mainNav">
      <div class="container px-4">
        <a class="navbar-brand" href="{{ path('app_home') }}#page-top">MarieTeam</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation"><span class="navbar-toggler-icon"></span></button>
        <div class="collapse navbar-collapse" id="navbarResponsive">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item">
              <a class="nav-link" href="#about">Nos bateaux</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#contact">Nous contacter</a>
            </li>
            {% if not app.user %}
              <li class="nav-item">
                <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
              </li>
            {% else %}
              <li class="nav-item">
                <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
              </li>
            {% endif %}
          </ul>
        </div>
      </div>
    </nav>
    {% block content %}
      <!-- Header -->
      <header class="bg-info bg-gradient text-white">
        <div class="container px-4 text-center">
          <h1 class="fw-bolder">Bienvenue sur MarieTeam</h1>
          <p class="lead">Réservez dès maintenant votre traversée !</p>
          <a class="btn btn-lg btn-light" href="{{ path('reservation_new_step_1') }}">Réservez ici</a>
        </div>
      </header>
      <!-- About section -->
      <section id="about">
        <div class="container px-4">
          <div class="row gx-4 justify-content-center">
            <div class="col-lg-8">
              <h2>Nos bateaux</h2>
              <div class="row row-cols-1 row-cols-md-3 g-4">
                {% for boat in boats %}
                  <div class="col">
                    <div class="card h-100">
                      <img src="{{ asset('build/images/boat.jpg') }}" class="card-img-top" alt="..." />
                      <div class="card-body">
                        <h5 class="card-title">{{ boat.libelleBateau }}</h5>
                        <ul class="card-text">
                          <li>Longueur: {{ boat.longueur }} mètres</li>
                          <li>Largeur: {{ boat.largeur }} mètres</li>
                          <li>Vitesse: {{ boat.vitesse }} noeuds</li>
                          <li>Capacité: {{ boat.capaciteMaximum }} personnes</li>
                        </ul>
                      </div>
                      <div class="card-footer">
                        <small class="text-muted">{{ boat.traversees|length }} traversées existent avec ce bateau.</small>
                      </div>
                    </div>
                  </div>
                {% endfor %}
              </div>
            </div>
          </div>
        </div>
      </section>
      <!-- Contact section -->
      <section id="contact">
        <div class="container px-4">
          <div class="row gx-4 justify-content-center">
            <div class="col-lg-8">
              <h2>Nous contacter</h2>
              <p class="lead">Lorem ipsum dolor sit amet, consectetur adipisicing elit. Vero odio fugiat voluptatem dolor, provident officiis, id iusto! Obcaecati incidunt, qui nihil beatae magnam et repudiandae ipsa exercitationem, in, quo totam.</p>
            </div>
          </div>
        </div>
      </section>
    {% endblock %}
    <!-- Footer -->
    <footer class="py-3 bg-dark fixed-bottom">
      <div class="container px-4">
        <p class="m-0 text-center text-white">Copyright &copy; MarieTeam 2023</p>
      </div>
    </footer>
    {% block javascripts %}
      {{ encore_entry_script_tags('app') }}
    {% endblock %}
  </body>
</html>
